#!/bin/bash

TOOLS_IMAGE="opencanarias/taple-tools:0.3"
TOOL="taple-patch"

ARGS=()
for var in "$@"
do
    ARGS+=("$var")
done

docker run --rm --name docker-taple-sign ${TOOLS_IMAGE} ${TOOL} "${ARGS[@]}"
